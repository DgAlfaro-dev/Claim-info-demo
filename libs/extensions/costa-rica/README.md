# costa-rica

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build costa-rica` to build the library.

## Running unit tests

Run `nx test costa-rica` to execute the unit tests via [Jest](https://jestjs.io).
